[package]
name = "boxes_sim"
description = "Boxes simulation kernel (sparse chunks, tick scheduler)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
